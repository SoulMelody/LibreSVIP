#!/bin/bash
poetry export -f requirements.txt --output ../deploy/setup/requirements.txt --without-urls --without-hashes --without=desktop,dev,test,code_gen,ffmpeg,packaging --with=webui,ujson,midi,subtitle,protobuf,text,svg,xml,binary